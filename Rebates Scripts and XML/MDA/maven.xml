<project default="GDXDist" xmlns:m="jelly:maven">

        <goal name="GDXDist">
                <attainGoal name="ear:ear"/>
                <echo>context root = ${ear.appxml.war.context-root}</echo>
                <echo>${maven.ear.appxml.version}</echo>
        </goal>
        <!--preGoal name="ear:init">
                <echo>${basedir}</echo>
                <copy
                        file="${basedir}/META-INF/application.xml"
                        todir="${basedir}/target"/>
        </preGoal-->
        <postGoal name="ear:ear">
                <copy
                        file="${basedir}/target/MDA.ear"
                        todir="${maven.home.local}/repository/${caremark.groupid}/ears"/>
        </postGoal>

</project>
