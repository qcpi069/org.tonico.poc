<!-- REFESH QL_CLT_MODEL  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
	]]></command>
	
	       
	<command type="try">
		<try>		    		
			<!-- Copy client info from Oracle to DB2. -->
			<command type="run" datasource="edwdb" rowName="count1">
				<![CDATA[
					select count(*)
					from (select  a.CLNT_ID,
      							    a.GRP_CLS_CD,
      							    a.GRP_CLD_CD_SHRT_TXT,
     							      a.GRP_CLS_CD_LONG_TXT
						FROM (
							select distinct a.CLNT_ID,
      								a.GRP_CLS_CD,
       								a.GRP_CLD_CD_SHRT_TXT,
     								  a.GRP_CLS_CD_LONG_TXT,
     								row_NUMBER() OVER (PARTITION BY a.CLNT_ID,a.GRP_CLS_CD ORDER BY a.CLNT_ID,a.GRP_CLS_CD) AS ROW_NUM 
							FROM DWCORP.T_QL_RPRT_GRP_CLS_CD a ) a 
						WHERE a.ROW_NUM = 1  )
                ]]>
			</command>
			<command type="copy" batch="5000">
				<source type="sql" datasource="edwdb"><![CDATA[
					select      a.CLNT_ID,
      							  a.GRP_CLS_CD, 
       							  a.GRP_CLD_CD_SHRT_TXT,
     							    a.GRP_CLS_CD_LONG_TXT
						FROM (
							select distinct a.CLNT_ID,
      								a.GRP_CLS_CD,
       								a.GRP_CLD_CD_SHRT_TXT,
     								  a.GRP_CLS_CD_LONG_TXT,
     								row_NUMBER() OVER (PARTITION BY a.CLNT_ID,a.GRP_CLS_CD ORDER BY a.CLNT_ID,a.GRP_CLS_CD) AS ROW_NUM 
							FROM DWCORP.T_QL_RPRT_GRP_CLS_CD a ) a 
						WHERE a.ROW_NUM = 1   
				]]></source>
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "5000"
					reportInterval   = "5000"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO rps.TQL_TREPORT_GRP_CLS_CD
					 (CLIENT_ID,
						GROUP_CLASS_CD,
						GRP_CLS_CD_SHT_TX,
						GRP_CLS_CD_LNG_TX				  
					  ) 
					VALUES ( ?, ?, ?, ?)
				]]></target>
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM rps.TQL_TREPORT_GRP_CLS_CD 
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("ZEUS PROD CLAIMSA.TREPORT_GRP_CLS_CD = " + count1);
				logger.warn("RPS RPS.TQL_TREPORT_GRP_CLS_CD COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" CLIENT counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" CLIENT counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPDM_TQL_TREPORT_GRP_CLS_CD_Refresh"
				subject="[ERROR] REFESH TQL_TREPORT_GRP_CLS_CD "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFESH TQL_TREPORT_GRP_CLS_CD</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFESH TQL_TREPORT_GRP_CLS_CD </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
