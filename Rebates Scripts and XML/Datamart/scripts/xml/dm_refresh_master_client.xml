
<!-- REFRESH MASTER_CLIENT  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");       
	]]></command>
		       
	<command type="try">
		<try>
            <!-- *************** MASTER_CLIENT ********************* -->
			<command type="run" datasource="rebates" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.master_client
				]]>
			</command>
			<!-- Copy master_client info from Oracle to DB2. -->
			<command type="copy" batch="1000">
				<source type="sql" datasource="rebates"><![CDATA[
					SELECT 
					  PCS_SYSTEM_ID,
					  MSTR_CLIENT_NAM,
					  DEFAULT_PAYMENT_SCHEDULE,
					  DEFAULT_AP_NBR,
					  DEFAULT_AP_COMPNY_CD,
					  DEFAULT_ADDR_LINE_1,
					  DEFAULT_ADDR_LINE_2,
					  DEFAULT_STATE,
					  DEFAULT_CITY,
					  DEFAULT_ZIP_CD,
					  DEFAULT_COUNTRY,
					  DEFAULT_CNTRC_EFF_DT,
					  DEFAULT_CNTRC_TERM_DT,
					  LAST_UPDATE_DT,
					  UPDATE_USERID,
					  INSRT_DATE,
					  ACCT_MGR,
					  MRKT_SEG_CD,
					  DEFAULT_PAYMENT_TYPE_CD,
					  DEFAULT_FAF_NBR,
					  DEFAULT_MODEL_TYPE_CD,
					  DEFAULT_PAYMENT_TIMING,	
					   DEFAULT_CUSTOM_RPT
					FROM ${cli_schema}.master_client 
						ORDER BY 1
				]]></source>
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "1000"
					reportInterval   = "1000"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO ${cli_schema}.master_client 
					 (PCS_SYSTEM_ID,
					  MSTR_CLIENT_NAM,
					  DEFAULT_PAYMENT_SCHEDULE,
					  DEFAULT_AP_NBR,
					  DEFAULT_AP_COMPNY_CD,
					  DEFAULT_ADDR_LINE_1,
					  DEFAULT_ADDR_LINE_2,
					  DEFAULT_STATE,
					  DEFAULT_CITY,
					  DEFAULT_ZIP_CD,
					  DEFAULT_COUNTRY,
					  DEFAULT_CNTRC_EFF_DT,
					  DEFAULT_CNTRC_TERM_DT,
					  LAST_UPDATE_DT,
					  UPDATE_USERID,
					  INSRT_DATE,
					  ACCT_MGR,
					  MRKT_SEG_CD,
					  DEFAULT_PAYMENT_TYPE_CD,
					  DEFAULT_FAF_NBR,
					  DEFAULT_MODEL_TYPE_CD,
					  DEFAULT_PAYMENT_TIMING,	
					   DEFAULT_CUSTOM_RPT
					  ) 
					VALUES ( ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? )
				]]></target>
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.master_client
				]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("Silver RBATE_REG.MASTER_CLIENT COUNT = " + count1);
				logger.warn("RPS RBATE_REG.MASTER_CLIENT COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" MASTER_CLIENT counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" MASTER_CLIENT counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPDM_Client_Refresh"
				subject="[ERROR] REFESH CLIENT - MASTER_CLIENT "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFRESH CLIENT - MASTER_CLIENT</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFRESH CLIENT - MASTER_CLIENT </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
