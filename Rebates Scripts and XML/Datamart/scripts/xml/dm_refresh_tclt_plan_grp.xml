
<!-- REFESH QL_CLT_MODEL  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");        
	]]></command>
	
	       
	<command type="try">
		<try>
		    
			<command type="run" datasource="gdx" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM vrap.TCLT_PLAN_GRP 
                ]]>
			</command>
			<!-- Copy client info from Oracle to DB2. -->
			<command type="copy" batch="500">
				<source type="sql" datasource="gdx"><![CDATA[
					SELECT 
						CLT_PLAN_GRP_ID,
						CLT_ID,
						PLAN_CD,
						PLAN_EXT_CD,
						GRP_CD,
						GRP_EXT_CD,
						BILLG_RPT_CD,
						PLAN_GRP_NM
												
					FROM vrap.TCLT_PLAN_GRP 
				]]></source>
				<target type="queue" threads="2">
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "500"
					reportInterval   = "1000"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO rps.TCLT_PLAN_GRP
					 (CLT_PLAN_GRP_ID,
						CLT_ID,
						PLAN_CD,
						PLAN_EXT_CD,
						GRP_CD,
						GRP_EXT_CD,
						BILLG_RPT_CD,
						PLAN_GRP_NM					  
					  ) 
					VALUES ( ?, ?, ?, ?, ?, ?, ?, ?)
				]]></target>
				</target>
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM rps.TCLT_PLAN_GRP 
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("GDX PROD VRAP.TCLT_PLAN_GRP COUNT = " + count1);
				logger.warn("RPS RPS.TCLT_PLAN_GRP COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" CLIENT counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" CLIENT counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPDM_QL_CLT_PLAN_GRP_Refresh"
				subject="[ERROR] REFESH QL_CLT_PLAN_GRP "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFESH TCLT_PLAN_GRP</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFESH TCLT_PLAN_GRP </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
