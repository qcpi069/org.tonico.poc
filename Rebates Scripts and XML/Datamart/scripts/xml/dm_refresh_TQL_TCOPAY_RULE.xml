<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");
	]]></command>
	
	       
	<command type="try">
		<try>
		    <!-- delete from UDB -->
			<command type="run" datasource="zeus" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM CLAIMSA.TCOPAY_RULE 
                ]]>
			</command>
			<!-- Copy zeus data to DB2. -->
			<command type="copy" batch="3000">
				<source type="sql" datasource="zeus"><![CDATA[
					SELECT PBA_ID, 
						COPAY_MIN_FILL_QY, 
						COPAY_MIN_COV_AT, 
						COPAY_MIN_AT, 
						COPAY_MAX_FILL_QY, 
						COPAY_MAX_COV_AT, 
						COPAY_MAX_AT, 
						COPAY_BASIS_CD, 
						RATE_AMT_RSOL_CD, 
						COPAY_STEP_TYP_CD, 
						PERIOD_TYPE_CD, 
						ACCOUNT_CLASS_CD, 
						ACCM_CLASS_CD, 
						HSC_TS, 
						COP_MIN_COV_ST2_AT, 
						COP_MAX_COV_ST2_AT, 
						PERIOD_TYPEST2_CD, 
						ACCOUNT_CLS_ST2_CD, 
						ACCM_CLASS_ST2_CD, 
						COPAY_LIMIT_ST1_AT, 
						COPAY_LIMIT_ST2_AT, 
						COPAY_CXO_ST1_IN, 
						COPAY_CXO_ST2_IN, 
						COPAY_AJST_ST1_CD, 
						COPAY_AJST_ST2_CD, 
						COPAY_TYPE_ST5_CD, 
						ORGNL_BATCH_GID, 
						UPD_BATCH_GID 
					FROM CLAIMSA.TCOPAY_RULE 
				]]></source>

                                <!--<target
				        type        = "queue"
					threadCount = "1">-->

				
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "false"
					batch            = "3000"
					reportInterval   = "3000"
					queryTimeout     = "${TIMEOUT}"
					><![CDATA[
						INSERT INTO RPS.TQL_TCOPAY_RULE
						VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?) 
				]]></target>
                                <!--</target> -->
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM RPS.TQL_TCOPAY_RULE 
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("Zeus TCOPAY_RULE COUNT = " + count1);
				logger.warn("RPS.TQL_TCOPAY_RULE COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" Record counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" Record counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
		 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="dm_refresh_TQL_TCOPAY_RULE"
				subject="[ERROR] REFESH TQL_TCOPAY_RULE "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFESH TQL_TCOPAY_RULE</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFESH TQL_TCOPAY_RULE </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
