
<!-- REFRESH rps.t_advance_base  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");        
	]]></command>
		       
	<command type="try">
		<try>
            <!-- *************** rps.t_advance_base ********************* -->
			<command type="copy" batch="1000">
				<source type="sql" datasource="datamart"><![CDATA[
select 
(case when c.rbat_id>9999999 then trim(char(c.rbat_id))
when c.rbat_id>999999 then '0'||trim(char(c.rbat_id))
when c.rbat_id>99999 then '00'||trim(char(c.rbat_id))
else  '000'||trim(char(c.rbat_id)) end),
	rac.rac_id, ta.PRC_TERM_ADV_PMT_ID as ADV_PMT_ID,
        c.eff_dt, c.term_dt, ta.STTL_TYP_CD,
        r.adv_pmt_tm_cd as PMT_FREQ,
        ta.calc_typ_cd, calc_shr_amt,
        date(ta.UPDT_TS) as LAST_UPDT_DT, ta.UPDT_USER_ID as LAST_UPDT_USER_NB,
        modl_vl as CLCT_FCTR_VL,
        adv_pmt_schd_cd as PMT_SCHD_CD
	,ta.PRC_TYP_CD
from client_reg.crt_clnt_rule_assc c
      join client_reg.crt_clnt_prc_pool_assc rac
                on c.rbat_id=rac.rbat_id
      join client_reg.crt_prc_rule r
                on r.prc_rule_id = c.prc_rule_id
      join client_reg.crt_prc_pool p
                on p.prc_rule_id = c.prc_rule_id and p.pool_typ_cd = rac.pool_typ_cd
      join client_reg.crt_prc_term_adv_pmt ta
                on ta.prc_pool_id = p.prc_pool_id
where c.eff_dt<c.term_dt

				]]></source>
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "1000"
					reportInterval   = "1000"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO rps.t_advance_base
                                         (REBATE_ID,
                                          RAC_ID,
                                          PRC_TERM_ADV_PMT_ID,
                                          EFF_DT,
                                          TERM_DT,
                                          STTL_TYP_CD,
                                          ADV_PMT_TM_CD,
                                          CALC_TYP_CD,
                                          CALC_SHR_AMT,
                                          LAST_UPDT_DT,
                                          UPDT_USER_ID,
                                          MODL_VL,
                                          ADV_PMT_SCHD_CD
					,PRC_TYP_CD
					  ) 
					VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?)
				]]></target>
			</command>
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="helen.tang@caremark.com"
				subject="[ERROR] GDX -> DM REFESH - rps.t_advance_base "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] GDX -> DM REFRESH - rps.t_advance_base</title>
				</head>
				<body>
			
				<h3><u>[ERROR] GDX -> DM REFRESH - rps.t_advance_base </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
		
        </command>
</sqml>

