
<!-- REFESH CLIENT  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");
	]]></command>
	
	       
	<command type="try">
		<try>
		    <!-- delete from UDB -->
			<command type="run" datasource="datamart">
				<![CDATA[
					DELETE FROM ${cli_schema}.client 
				]]>
			</command>
			<command type="run" datasource="rebates" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.client 
                ]]>
			</command>
			<!-- Copy client info from Oracle to DB2. -->
			<command type="copy" batch="1000">
				<source type="sql" datasource="rebates"><![CDATA[
					SELECT 
					  LPAD(TRIM(CLIENT_NBR),8,'0'),
					  PCS_SYSTEM_ID,
					  LPAD(TRIM(REPORT_GROUP_NBR),8,'0'),
					  CLIENT_NAME, 
					  AKA_NAME,
					  HEALTH_IND_NBR,
					  CLNT_XFER_DT,
					  CLNT_NEW_NB,
					  UPDATE_USERID,
					  INSRT_DATE,
					  PROCESSOR_CD,
					  CLNT_NB_TYPE_CD,
					  AP_COMPNY_CD,					  
					  FAF_NBR,					 
					  QL_CLIENT_NBR,
					  SAP_PAYMENT_TYPE,
					  CREDIT_HOLD_CD,
					  CLIENT_TYPE,
					  PAYMENT_TYPE,
					  PAYMENT_TIMING,
					  PAYMENT_SCHEDULE,
					  AP_NBR,
					  BUSINESS_UNIT_CD, 
					  REBATE_TYPE,
                                          MAIL_AT_RETAIL, 
                                          RBEL_CLNT_CD, 
                                          PMT_SYS_ELIG_CD
					FROM ${cli_schema}.client clnt
						ORDER BY 1,2
				]]></source>
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "1000"
					reportInterval   = "1000"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO ${cli_schema}.client
					 (CLIENT_NBR,
					  PCS_SYSTEM_ID,
					  REPORT_GROUP_NBR,
					  CLIENT_NAME,
					  AKA_NAME,
					  HEALTH_IND_NBR,
					  CLNT_XFER_DT,
					  CLNT_NEW_NB,
					  UPDATE_USERID,
					  INSRT_DATE,
					  PROCESSOR_CD,
					  CLNT_NB_TYPE_CD,
					  AP_COMPNY_CD,					  
					  FAF_NBR,					 
					  QL_CLIENT_NBR,
					  SAP_PAYMENT_TYPE,
					  CREDIT_HOLD_CD,
					  CLIENT_TYPE,
					  PAYMENT_TYPE,
					  PAYMENT_TIMING,
					  PAYMENT_SCHEDULE,
					  AP_NBR,
					  BUSINESS_UNIT_CD,
					  REBATE_TYPE,
                                          MAIL_AT_RETAIL,
                                          RBEL_CLNT_CD, 
                                          PMT_SYS_ELIG_CD
					  ) 
					VALUES ( ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,?, ?, ?,? )
				]]></target>
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.client 
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("Silver RBATE_REG.CLIENT COUNT = " + count1);
				logger.warn("RPS RBATE_REG.CLIENT_COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" CLIENT counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" CLIENT counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPDM_Client_Refresh"
				subject="[ERROR] REFESH CLIENT "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFESH CLIENT</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFESH CLIENT </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
