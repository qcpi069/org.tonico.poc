<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");
	]]></command>
	
	       
	<command type="try">
		<try>
		     	<command type="run" datasource="zeus" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM CLAIMSA.TPB_RXBEN_ATT_HIST 
                ]]>
			</command>
			<!-- Copy data from Zeus to DB2. -->
			<command type="copy" batch="3000">
				<source type="sql" datasource="zeus"><![CDATA[
					SELECT PB_ID, 
						PBA_ID, 
						PBR_NETWORK_ID, 
						PHM_NETWORK_ID, 
						DRUG_CLASS_ID, 
						EFF_DT, 
						EXP_DT, 
						PB_STATUS_CD, 
						PBA_CLASS_CD, 
						PBA_PRIORITY_NB, 
						HSC_TS, 
						HSU_TS, 
						ORGNL_BATCH_GID, 
						UPD_BATCH_GID
					FROM CLAIMSA.TPB_RXBEN_ATT_HIST 
				]]></source>

                                <!--<target
				        type        = "queue"
					threadCount = "1">-->

				
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "false"
					batch            = "3000"
					reportInterval   = "3000"
					queryTimeout     = "${TIMEOUT}"
					><![CDATA[
						INSERT INTO RPS.TQL_TPB_RXBEN_ATT_HIST
						VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?) 
				]]></target>
                               <!-- </target> -->
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM RPS.TQL_TPB_RXBEN_ATT_HIST 
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("Zeus TPB_RXBEN_ATT_HIST = " + count1);
				logger.warn("RPS.TQL_TPB_RXBEN_ATT_HIST = " + count2);
				if (count1 != count2) {
					logger.error(" Record counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" Record counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPS_TQL_TPB_RXBEN_ATT_HIST"
				subject="[ERROR] REFESH RPS.TQL_TPB_RXBEN_ATT_HIST "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFESH RPS.TQL_TPB_RXBEN_ATT_HIST</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFESH RPS.TQL_TPB_RXBEN_ATT_HIST </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
