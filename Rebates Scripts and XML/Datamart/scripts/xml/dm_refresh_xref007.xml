
<!-- REFRESH XREF007C  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        dmt_schema=script.getProperty("dmt_schema");
		pmt_schema=script.getProperty("pmt_schema");
	]]></command>
		       
	<command type="try">
		<try>
            <!-- *************** CG XREF ********************* -->
			<!-- Copy cg xref info from mainframe  -->
			<command type="copy" batch="500">
				<source type="sql" datasource="db2p"><![CDATA[
					SELECT 
                       CG_NB,
					   EXTL_SRC_CD,
					   EXTL_HRCY_LVL1,
					   EXTL_HRCY_LVL2, 
					   EXTL_HRCY_LVL3, 
					   EXTL_HRCY_LVL4, 
					   EXTL_HRCY_LVL5 					 
					FROM ${pmt_schema}.xref007 
						ORDER BY 1
				]]></source>
			<target type= "queue" threads="4" >  
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "500"
					reportInterval   = "500"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO ${dmt_schema}.xref007 
					 (CG_NB,
					  EXTL_SRC_CD,
					  EXTL_HRCY_LVL1,
					  EXTL_HRCY_LVL2,
					  EXTL_HRCY_LVL3,
					  EXTL_HRCY_LVL4,
					  EXTL_HRCY_LVL5 
					  ) 
					VALUES ( ?, ?, ?, ?, ?, ?, ? )
				]]></target>
             </target>
			</command>
	        <!-- compare totals -->
			<command type="run" datasource="db2p" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM ${pmt_schema}.xref007
                ]]>
			</command>
			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM ${dmt_schema}.xref007
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("mainframe xref007 COUNT = " + count1);
				logger.warn("RPS xref007  COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" xref007  counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" xref007  counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPDM_Refresh"
				subject="[ERROR] REFESH - XREF007  "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFESH - XREF007</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFESH - XREF007 </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
