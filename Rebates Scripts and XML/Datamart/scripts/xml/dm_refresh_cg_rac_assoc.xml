
<!-- REFRESH CG_RAC_ASSOC  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");
	]]></command>
		       
	<command type="try">
		<try>
            <!-- *************** CG_RAC_ASSOC ********************* -->
			<command type="run" datasource="rebates" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.cg_rac_assoc
                ]]>
			</command>
			<!-- Copy cg_rac_assoc info from Oracle to DB2. -->
			<command type="copy" batch="500">
				<source type="sql" datasource="rebates"><![CDATA[
					SELECT 
					  EXTL_SRC_CD,
					  EXTL_LVL1_ID,
					  EXTL_LVL2_ID,
					  EXTL_LVL3_ID,
					  EXTL_LVL4_ID,
					  EXTL_LVL5_ID,
					  RAC,
					  RAC_EFF_DT,
					  RAC_TERM_DT,
					  UPDATE_USERID,
					  INSRT_DATE 
					FROM ${cli_schema}.cg_rac_assoc 
						ORDER BY 1,2,3,4,5,6,7,8
				]]></source>
		     <target type= "queue" threads="6" > 
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "500"
					reportInterval   = "500"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO ${cli_schema}.cg_rac_assoc 
					 (EXTL_SRC_CD,
					  EXTL_LVL1_ID,
					  EXTL_LVL2_ID,
					  EXTL_LVL3_ID,
					  EXTL_LVL4_ID,
					  EXTL_LVL5_ID,
					  RAC,
					  RAC_EFF_DT,
					  RAC_TERM_DT,
					  UPDATE_USERID,
					  INSRT_DATE		
					  ) 
					VALUES ( ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? )
				]]></target>
			 </target>
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.cg_rac_assoc
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("Silver RBATE_REG.CG_RAC_ASSOC COUNT = " + count1);
				logger.warn("RPS RBATE_REG.CG_RAC_ASSOC COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" CG_RAC_ASSOC counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" CG_RAC_ASSOC counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPDM_Client_Refresh"
				subject="[ERROR] REFESH CLIENT - CG RAC ASSOC "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFRESH CLIENT - CG RAC ASSOC</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFRESH CLIENT - CG RAC ASSOC </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
