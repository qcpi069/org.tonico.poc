
<!-- REFRESH ADV_PMTS  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");
	]]></command>
		       
	<command type="try">
		<try>
            <!-- *************** Advance Payments ********************* -->
			<command type="run" datasource="rebates" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.adv_pmnts
                ]]>
			</command>
			<!-- Copy adv_pmts info from Oracle to DB2. -->
			<command type="copy" batch="1000">
				<source type="sql" datasource="rebates"><![CDATA[
					SELECT 
					  ADV_PMNT_ID,
					  CLT_PCS_SYSTEM_ID,
					  PMNT_TIMING,
					  LEAD_DAYS,
					  EFF_DT,
					  TERM_DT,
					  RAC_PCS_SYSTEM_ID,
					  CALC_TYPE,
					  CALC_SHR,
					  MODULATOR,
					  LAST_UPDATE_DT,
					  UPDATE_USERID,
					  INSRT_DATE,
					  SETLMNT_TYPE_CD,
                                          PAYMENT_SCHEDULE
					FROM ${cli_schema}.adv_pmnts 
						ORDER BY 1, 2
				]]></source>
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "1000"
					reportInterval   = "1000"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO ${cli_schema}.adv_pmnts
					 (
					  ADV_PMNT_ID,
					  CLT_PCS_SYSTEM_ID,
					  PMNT_TIMING,
					  LEAD_DAYS,
					  EFF_DT,
					  TERM_DT,
					  RAC_PCS_SYSTEM_ID,
					  CALC_TYPE,
					  CALC_SHR,
					  MODULATOR,
					  LAST_UPDATE_DT,
					  UPDATE_USERID,
					  INSRT_DATE,
					  SETLMNT_TYPE_CD,
                                          PMT_SCHD_CD    			
					  ) 
					VALUES ( ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? )
				]]></target>
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.adv_pmnts
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("Silver RBATE_REG.ADV_PMNTS COUNT = " + count1);
				logger.warn("RPS RBATE_REG.ADV_PMNTS COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" ADV_PMNTS counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" ADV_PMNTS counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
				System.out.println(" *********************************************** ");
				System.out.println(" * Rows refreshed into RBATE_REG.ADV_PMNTS is " + count1 + ". ");
				System.out.println(" *********************************************** ");
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPDM_Client_Refresh"
				subject="[ERROR] REFESH CLIENT - ADV_PMTS "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFRESH CLIENT - ADV_PMNTS</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFRESH CLIENT - ADV_PMNTS </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
