 
<!-- REFESH MAINFRAME PAYMENTS SETTLEMENTS TABLE  -->
  <!-- UPDATE_USERID,  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

		   
     <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");
        pmt_schema=script.getProperty("pmt_schema");
	]]></command>
	

	<command type="try">
		<try>
			<!-- delete from DB2. -->
			<command type="run" datasource="db2p">
				<![CDATA[
					DELETE FROM ${pmt_schema}.kscc212_sttlmt
				]]>
			</command>
     
			<!-- Copy from Oracle to mainframe DB2. -->
			<command type="copy" batch="1000">
				<source type="sql" datasource="rebates"><![CDATA[
        SELECT 
			NVL(lpad(rtrim(clnt.client_nbr,' '),8,'0'),'00000000') AS rbate_id,
			SETLMNT_ID,
			SETLMNT_DT,
			NVL( lpad(rtrim(SETLMNT_TYPE,' '),1,' '),' ') AS SETLMNT_TYPE,
			SETLMNT_COMP_DT,
			stl.LAST_UPDATE_DT,
			stl.UPDATE_USERID,
			stl.INSRT_DATE
         FROM  ${cli_schema}.setlmnts stl,
               ${cli_schema}.client clnt
         WHERE clnt.PCS_SYSTEM_ID = stl.PCS_SYSTEM_ID
           order by 1, 2, 3

				]]></source> 
				<target
					type             = "sql"
					datasource       = "db2p"
					transaction      = "true"
					batch            = "1000"
					reportInterval   = "1000"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO ${pmt_schema}.kscc212_sttlmt
                    (RBAT_ID,   
					 STTL_ID,
					 STTL_DT,
					 STTL_TYP_CD,
					 STTL_CMPL_DT,
					 LAST_UPDT_DT,
					 LAST_UPDT_USER_NB,
					 INSRT_DT
                     )
					VALUES ( ?, ?, ?, ?, ?, ?, ?, ? )
				]]></target>
			</command>
    
	        <!-- compare totals -->
			<command type="run" datasource="rebates" rowName="count1">
				<![CDATA[
	SELECT COUNT(*) FROM ( 
         SELECT 
			NVL(lpad(rtrim(clnt.client_nbr,' '),8,'0'),'00000000') AS rbate_id,
			SETLMNT_ID,
			SETLMNT_DT,
			NVL( lpad(rtrim(SETLMNT_TYPE,' '),1,' '),' ') AS SETLMNT_TYPE,
			SETLMNT_COMP_DT,
			stl.LAST_UPDATE_DT,
			stl.UPDATE_USERID,
			stl.INSRT_DATE
         FROM  ${cli_schema}.setlmnts stl,
               ${cli_schema}.client clnt
         WHERE clnt.PCS_SYSTEM_ID = stl.PCS_SYSTEM_ID   
)
				]]>
			</command>

			<command type="run" datasource="db2p" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM ${pmt_schema}.kscc212_sttlmt
				]]>
			</command>
   
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();  
				count2 = script.getProperty("count2.1").intValue();
				if (count1 != count2) {
					logger.error(" kscc212_sttlmt counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" kscc212_sttlmt counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
				System.out.println(" *********************************************** ");
				System.out.println(" * Rows refreshed into KSCC212 is " + count1 + ". ");
				System.out.println(" *********************************************** ");
			</command>
			 
		</try>
		<catch>
			<!-- Send an email on error -->
		<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPMF_Refresh_Client"
				subject="[ERROR] MF REFESH KSCC212 "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] MF REFRESH KSCC212</title>
				</head>
				<body>
			
				<h3><u>[ERROR] MF REFRESH KSCC212 </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		
		
		</catch>
	</command>
</sqml>
