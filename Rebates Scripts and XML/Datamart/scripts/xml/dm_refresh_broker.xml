
<!-- REFRESH BROKER  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>     
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");
	]]></command>

	<command type="try">
		<try>
			<command type="run" datasource="rebates" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.broker 
                ]]>
			</command>
			<!-- Copy client info from Oracle to DB2. -->
			<command type="copy" batch="500">
				<source type="sql" datasource="rebates"><![CDATA[
					SELECT 
					  LPAD(TRIM( BROKER_ID),8,'0'),
					  BROKER_NAM,
					  AP_NBR,
					  AP_COMPNY_CD,
					  LAST_UPDATE_DT,
					  UPDATE_USERID,
					  INSRT_DATE,
					  SAP_TYPE 
					FROM ${cli_schema}.broker
						ORDER BY broker_id
				]]></source>
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "500"
					reportInterval   = "500"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO ${cli_schema}.broker
					 (BROKER_ID,
					  BROKER_NAM,
					  AP_NBR,
					  AP_COMPNY_CD,
					  LAST_UPDATE_DT,
					  UPDATE_USERID,
					  INSRT_DATE,
					  SAP_TYPE 
					  ) 
					VALUES ( ?, ?, ?, ?, ?, ?, ?, ? )
				]]></target>
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM ${cli_schema}.broker
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("Silver BROKER COUNT = " + count1);
				logger.warn("RPS BROKER   COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" BROKER counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" BROKER counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPDM_Client_Refresh"
				subject="[ERROR] REFESH CLIENT - BROKER "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFESH CLIENT - BROKER</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFESH CLIENT - BROKER</u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
