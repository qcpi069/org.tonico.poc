
<!-- BSR 43919 updated 04/23/2009 by qcpi03o -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        cli_schema=script.getProperty("cli_schema");        
	]]></command>
		       
	<command type="try">
		<try>
			<command type="copy" batch="1000">
				<source type="sql" datasource="edwdb"><![CDATA[
select claim_gid, mbr_id, bnft_lvl_code
from dwcorp.t_claim partition (p_${QPART}) c
,dma_rbate2.v_mbr m
where c.algn_lvl_gid in (${ALGN_GID})
and c.mbr_gid=m.mbr_gid
				]]></source>
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "1000"
					reportInterval   = "1000"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO rps.BCBS_lookup
					 (CLAIM_GID,
					  MBR_ID,
					  PLAN_CD
					  ) 
					VALUES ( ?, ?,? )
				]]></target>
			</command>

		</try>
		<catch>
		</catch>
	</command>
</sqml>
