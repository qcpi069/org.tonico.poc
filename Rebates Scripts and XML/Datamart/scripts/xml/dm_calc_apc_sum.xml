
<!-- BUILD APC SUM  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
        dmt_schema=script.getProperty("dmt_schema");        
	]]></command>
			   
	<command type="try">
		<try>
			<command type="copy" batch="500">
				<source type="sql" datasource="datamart"><![CDATA[		
 Select  
	'${QPID}' As Qtr,
	pico_no, 
	cntrl_no, 
	NDC,  
	rbate_id,  
	extnl_src_code, 
	Count(claim_lvl1) as rx_count,
	Sum(unit_qty),  
	Sum(rbate_access) AS reb_acc,  
	Sum(rbate_mrkt_shr) AS reb_mkt, 
	Sum(rbate_admin_fee) AS admin, pmt_sys_elig_cd  
FROM 
	(SELECT * 
	 FROM rps.tapc_detail_${QPID}  
	  where excpt_stat = 'R' 
	  and (( rbate_access <> 0 ) or ( rbate_mrkt_shr <> 0 ) or ( rbate_admin_fee  <> 0 ))
	) a       
GROUP BY   
	pico_no, 
	cntrl_no,   
	NDC,  
	rbate_id, 
	extnl_src_code, pmt_sys_elig_cd  
				]]></source>
				<target type= "queue" threads="4" >  
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "500"
					reportInterval   = "500"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO ${dmt_schema}.tapc_sum 
					 (PERIOD_ID,
					  PICO_NO,
					  CNTRL_NO,
					  NDC,
					  RBATE_ID,
					  EXTNL_SRC_CD,
					  RX_COUNT,
					  UNIT_QTY, 
					  REB_ACC,
					  REB_MKT,
					  REB_ADM, pmt_sys_elig_cd   
					  ) 
					VALUES ( ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? , ?)    
				]]></target>
				</target>
			</command>

<!-- add aggregate step for inv_qtr/rebate_id/rac  -->
			<command type="copy" batch="500">
				<source type="sql" datasource="datamart"><![CDATA[		
	 SELECT  distinct PERIOD_ID, RBATE_ID, RAC
	 FROM rps.tapc_detail_${QPID}  
				 
				]]></source>
				<target type= "queue" threads="4" >  
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "500"
					reportInterval   = "500"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO ${dmt_schema}.t_apc_aggr 
					 (INV_QTR,
					  RBATE_ID,
					  RAC
					  ) 
					VALUES ( ?,?,?)    
				]]></target>
				</target>
			</command>

		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="rpsadm@caremark.com"
				subject="[ERROR] CALC APC SUM "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] CALC APC SUM</title>
				</head>
				<body>
			
				<h3><u>[ERROR] CALC APC SUM </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
