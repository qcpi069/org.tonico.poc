<!-- REFESH QL_CLT_MODEL  -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	<properties resource="datamart.properties"/>

    <command type="java"><![CDATA[
        mailto=script.getProperty("mailto");
 	]]></command>
	
	       
	<command type="try">
		<try>		    		
			<!-- Copy client info from Oracle to DB2. -->
			<command type="run" datasource="edwdb" rowName="count1">
				<![CDATA[
					SELECT COUNT(*) FROM (
					SELECT 
						CLNT_ID,
						PB_ID,
						PB_ROLE_CD,
						EFF_DT,
						EXPRN_DT,
						PB_STUS_CD,
						SRC_HSC_TS,
						SRC_HSU_TS
					from
					(SELECT 
						CLNT_ID,
						PB_ID,
						PB_ROLE_CD,
						EFF_DT,
						EXPRN_DT,
						PB_STUS_CD,
						to_date(to_char(SRC_HSC_TS,'mm/dd/yyyy hh24:mi:ss'),'mm/dd/yyyy hh24:mi:ss') as SRC_HSC_TS,
						to_date(to_char(SRC_HSU_TS,'mm/dd/yyyy hh24:mi:ss'),'mm/dd/yyyy hh24:mi:ss') as SRC_HSU_TS,
						row_NUMBER() OVER (PARTITION BY CLNT_ID,PB_ID,PB_ROLE_CD,EFF_DT,PB_STUS_CD ORDER BY CLNT_ID,PB_ID,PB_ROLE_CD,EFF_DT,PB_STUS_CD) AS ROW_NUM 
					FROM DWCORP.T_QL_CLNT_PB_ROLE_HIST ) a
					where a.ROW_NUM = 1  )
                ]]>
			</command>
			<command type="copy" batch="5000">
				<source type="sql" datasource="edwdb"><![CDATA[
					SELECT 
						CLNT_ID,
						PB_ID,
						PB_ROLE_CD,
						EFF_DT,
						EXPRN_DT,
						PB_STUS_CD,
						SRC_HSC_TS,
						SRC_HSU_TS
					from
					(SELECT 
						CLNT_ID,
						PB_ID,
						PB_ROLE_CD,
						EFF_DT,
						EXPRN_DT,
						PB_STUS_CD,
						to_date(to_char(SRC_HSC_TS,'mm/dd/yyyy hh24:mi:ss'),'mm/dd/yyyy hh24:mi:ss') as SRC_HSC_TS,
						to_date(to_char(SRC_HSU_TS,'mm/dd/yyyy hh24:mi:ss'),'mm/dd/yyyy hh24:mi:ss') as SRC_HSU_TS,
						row_NUMBER() OVER (PARTITION BY CLNT_ID,PB_ID,PB_ROLE_CD,EFF_DT,PB_STUS_CD ORDER BY CLNT_ID,PB_ID,PB_ROLE_CD,EFF_DT,PB_STUS_CD) AS ROW_NUM 
					FROM DWCORP.T_QL_CLNT_PB_ROLE_HIST ) a
					where a.ROW_NUM = 1 
				]]></source>
				<target
					type             = "sql"
					datasource       = "datamart"
					transaction      = "true"
					batch            = "5000"
					reportInterval   = "5000"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO rps.TQL_TCLIENT_PB_ROLE_HS
					 (CLIENT_ID,
						PB_ID,
						PB_ROLE_CD,
						EFF_DT,
						EXP_DT,
						PB_STATUS_CD,
						HSC_TS,
						HSU_TS				  
					  ) 
					VALUES ( ?, ?, ?, ?, ?, ?, ?,?)
				]]></target>
			</command>
	        <!-- compare totals -->

			<command type="run" datasource="datamart" rowName="count2">
				<![CDATA[
					SELECT COUNT(*) FROM rps.TQL_TCLIENT_PB_ROLE_HS 
                ]]>
			</command>
			<command type="java">
				count1 = script.getProperty("count1.1").intValue();
				count2 = script.getProperty("count2.1").intValue();
				logger.warn("GDX PROD QLDMA.S_QL_CPG_PLAN_BNFT_TRSLN_HIST = " + count1);
				logger.warn("RPS RPS.TQL_TCLIENT_PB_ROLE_HS COUNT = " + count2);
				if (count1 != count2) {
					logger.error(" CLIENT counts do NOT match count1=" + count1 + "  count2=" + count2);
                    throw new Exception(" CLIENT counts do NOT match count1=" + count1 + " count2=" + count2);
				} 
			</command> 
				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="${mailto}"
				mailFrom="RPDM_TQL_TCLIENT_PB_ROLE_HS_Refresh"
				subject="[ERROR] REFESH TQL_TCLIENT_PB_ROLE_HS "
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] REFESH TQL_TCLIENT_PB_ROLE_HS</title>
				</head>
				<body>
			
				<h3><u>[ERROR] REFESH TQL_TCLIENT_PB_ROLE_HST </u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
