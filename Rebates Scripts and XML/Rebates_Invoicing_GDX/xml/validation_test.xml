
<!-- REFESH INCENTIVE TYPE CODE -->

<sqml>
	
	<property name="TIMEOUT" value="0"/>
	
	<datasources resource="datasource.xml"/>
	
	<!--
		Set-up a try block to send an email on exception.	
	-->         
	<command type="try">
		<try>

			<command type="run" datasource="silver"><![CDATA[
				SELECT SYSDATE FROM dual
			]]></command>
			
			<command type="run" datasource="gdxprd" rowName="gdx_tfh_count"><![CDATA[
				SELECT COUNT(*) FROM CLAIMSP.TPB_FORMULARY_HIST
			]]></command>
			
			<command type="java"><![CDATA[
				System.out.println("Hello out there!");
				throw new Exception("validation test");
			]]></command>

		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="bryan.castillo@caremark.com"
				mailFrom="validation_test@caremark.com"
				subject="[${region}] [ERROR] VALIDATION TEST"
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>Validation Test</title>
				</head>
				<body>
			
				<h3><u>[ERROR] Validation Test</u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
