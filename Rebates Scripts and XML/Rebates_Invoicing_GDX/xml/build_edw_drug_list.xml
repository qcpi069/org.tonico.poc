<!-- COPIES DATA FROM IRON DMA.T_DRUG TO GDXPRD VRAP.TDRUG_EDW  -->
<!-- THE SCHEDULE THAT EXECUTES THIS LOADS EVERYDAY AND IS RUN WHEN A TRIGGER FILE BECOMES PRESENT -->

<sqml>
	<property name="TIMEOUT" value="0"/>
	<datasources resource="datasource.xml"/>
	
	<!--
		Set-up a try block to send an email on exception.	
	-->         
	<command type="try">
		<try>
			<!-- Clear out the table on GDXPRD. -->
			<command type="run" datasource="gdxprd" transaction="false">
				<![CDATA[
					DELETE FROM VRAP.TDRUG_EDW
				]]>
			</command>
			<!-- Copy data from IRON to GDXPRD -->
			<command type="copy" batch="500">
				<source type="sql" datasource="iron"><![CDATA[
    				        select
					   a.DRUG_GID,
					   a.GPI_CODE,
       					   a.GCN_NBR,
				           a.NDC_CODE,
       					   a.FDB_BRAND_NAME,
					   a.PRDCT_NAME,
       					   a.LBL_NAME,
       					   a.BRAND_NAME,
       					   a.DRUG_FDB_SRC_FLAG,
       					   a.DRUG_MDDB_SRC_FLAG,
       					   a.DRUG_RECAP_SRC_FLAG,
       					   a.DRUG_RXCLAIM_SRC_FLAG,
       					   a.GNRC_FLAG,
       					   a.MS_GNRC_FLAG,
       					   a.FDB_EFF_DATE,
       					   a.FDB_END_DATE,
       					   a.MDDB_EFF_DATE,
       					   a.MDDB_END_DATE,
       					   a.END_DATE
					from dma.t_drug a
					where a.eff_date=( select max(b.eff_date)
					                     from dma.t_drug b
					                    where b.ndc_code = a.ndc_code
					                      and b.obslt_flag is null)
					and (a.drug_fdb_src_flag = 1 or
					     a.drug_mddb_src_flag = 1 or	
					     a.drug_recap_src_flag = 1 or
					     a.drug_rxclaim_src_flag = 1)
				]]></source>
                                <target type="queue" threadCount="5">
				<target
					type             = "sql"
					datasource       = "gdxprd"
					transaction      = "false"
					batch            = "500"
					reportInterval   = "500"
					queryTimeout     = "${TIMEOUT}"
				><![CDATA[
					INSERT INTO VRAP.TDRUG_EDW ( DRUG_GID,
    									GPI_CODE,
    									GCN_NBR,
									NDC_LC11_ID,
    									FDB_BRAND_NAME,
    									PRDCT_NAME,
    									LBL_NAME,
    									BRAND_NAME,
    									DRUG_FDB_SRC_FLAG,
    									DRUG_MDDB_SRC_FLAG,
    									DRUG_RECAP_SRC_FLAG,
    									DRUG_RXCLAIM_SRC_FLAG,
    									GNRC_FLAG,
    									MS_GNRC_FLAG,
    									FDB_EFF_DT,
    									FDB_END_DT,
    									MDDB_EFF_DT,
   									MDDB_END_DT,
    									END_DT)
			                VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
				]]></target>
                                   </target>
			</command>				 
		</try>
		<catch>
			<!-- Send an email on error -->
			<command
				type="mail"
				mailTo="gdxitd@caremark.com"
				mailFrom="EDW_DRUG_BUILD@caremark.com"
				subject="[ERROR] BUILD_EDW_DRUG_LIST.XML"
				contentType="text/html"
				throwsException="false"
			><![CDATA[
				<html>
				<head>
				<title>[ERROR] BUILD_EDW_DRUG_LIST.XML</title>
				</head>
				<body>
			
				<h3><u>[ERROR] BUILD_EDW_DRUG_LIST.XML</u></h3>
			
				<table cellpadding="2">
					<tr><td><b>Host:</b></td><td>${localhost}<td></tr>
					<tr><td><b>Started:</b></td><td>${startTime}<td></tr>
					<tr><td><b>Script:</b></td><td>${scriptName}<td></tr>
					#if (${system.getProperty('logfile')})
						<tr>
							<td><b>Log File:</b></td>
							<td>
								${system.getProperty('logfile')}
							<td>
						</tr>
					#end
				</table>

				<br>			
				<br>
				
				#if (${script.getLastStatement()})
					<h3><u>Last Statement</u></h3>
					<font style="color: blue; font-size: small">	
					${script.getLastStatement()}
					</font>
				
					<br>
					<br>
				#end
			
				<h3><u>Stack Trace</u></h3>		
				<pre style="color: red; font-size: small;">${stackTrace}</pre>
				</body>
				</html>
			]]></command>
		</catch>
	</command>
</sqml>
